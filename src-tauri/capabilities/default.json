{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the trusted main application window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
